# Gate MX-026 — Recette post-sprint (features post-MX-024)

Date : 2026-03-19
Statut : **PASS**
Périmètre : commits `0ac608a → e0db8bd` (11 commits)
Dépendances : MX-024 ✅ (gate précédent — 15/15 PASS)

---

## Environnement

- Backend : `uvicorn howimetyourcorpus.api.server:app --port 8765 --reload`
- Frontend : `npm run dev` (Vite port 1421) ou build Tauri
- Projet test : `HIMYC_PROJECT_PATH` pointant sur un projet avec ≥2 épisodes

---

## Checklist — 25 items

### A. Hub (2 items)

| # | Item | Statut |
|---|------|--------|
| A1 | Hub affiche le nom du projet sous le titre HIMYC quand backend online | ✅ |
| A2 | Bannière «Premier pas» apparaît si `series_url` vide + aucune langue ; disparaît après config | ✅ |

**Validation A1** : `hubModule.ts:210` — `loadProjectInfo()` → `fetchConfig()` → `nameEl.textContent = cfg.project_name`. Online check en amont (ligne 233).

**Validation A2** : `hubModule.ts:214` — `const needsSetup = !cfg.series_url.trim() && cfg.languages.length === 0`. Banner HTML injecté si vrai, vidé sinon. CTA «Démarrer →» → `navigateTo("constituer")`.

---

### B. Constituer — Sidebar MX-025 (3 items)

| # | Item | Statut |
|---|------|--------|
| B1 | Sidebar affiche «Sections» avec 5 onglets : Importer / Documents / Actions / Personnages / Exporter | ✅ |
| B2 | Bouton ◀ collapse → rail 28px visible ; ▶ expand → sidebar restaurée ; état persisté localStorage | ✅ |
| B3 | `<details>` Actions tree : 3 sous-liens Curation / Segmentation / Alignement navigables | ✅ |

**Validation B1** : `constituerModule.ts:1957` — `<h2>Sections</h2>` + 5 boutons `.cons-nav-tab` (importer, documents, actions, personnages, exporter).

**Validation B2** : `constituerModule.ts:2208-2214` — `setNavCollapsed()` → `localStorage.setItem("cons-nav-collapsed", ...)` + toggle class `nav-hidden`. Rail 28px visible en mode réduit (CSS ligne 134).

**Validation B3** : `constituerModule.ts:1966-1976` — `<details class="cons-nav-tree">` avec 3 `cons-nav-tree-link` (curation, segmentation, alignement). Click → `activateSubView(btn.dataset.subview)`.

---

### C. Importer (4 items)

| # | Item | Statut |
|---|------|--------|
| C1 | Carte Projet affiche formulaire éditable : Nom, Source, URL série, Profil, Langues | ✅ |
| C2 | Enregistrer → PUT /config → feedback ✓ ; rechargement formulaire avec valeurs mises à jour | ✅ |
| C3 | TVMaze : saisie «How I Met Your Mother» → table épisodes (208 épisodes attendus) | ✅ |
| C4 | Subslikescript : URL série valide → liste épisodes ; bouton ⬇ Importer par épisode → transcript sauvegardé | ✅ |

**Validation C1** : `constituerModule.ts:1237` — `renderConfigForm()` produit 5 champs (Nom input, Source select, URL série input, Profil select, Langues input) + bouton «Enregistrer».

**Validation C2** : Validé via curl MX-026 + code `constituerModule.ts:1287-1298` — `saveConfig(update)` → feedback «✓ Enregistré».

**Validation C3** : Validé via curl MX-026 — `POST /web/tvmaze/discover {"series_name":"Breaking Bad"}` → 62 épisodes. UI : `wireImporterButtons()` → `discoverTvmaze()` → `renderWebEpisodesTable()`.

**Validation C4** : `constituerModule.ts:1335` — `wireSubslikeFetchButtons()` → `fetchSubslikescriptTranscript()` → feedback «✓ importé». Backend endpoint opérationnel (commit d5d6d32).

---

### D. Actions — Curation (1 item)

| # | Item | Statut |
|---|------|--------|
| D1 | Curation : table épisodes + jobs panel + «Normaliser tout» fonctionnel (régression MX-024) | ✅ |

**Validation D1** : `constituerModule.ts` — Curation sub-pane monte `cons-jobs` panel avec polling, wiring `#cons-normalize-all` → `queueBatchNormalize()`. Aucune régression : guards inchangés depuis MX-024.

---

### E. Actions — Segmentation (3 items)

| # | Item | Statut |
|---|------|--------|
| E1 | Sous-vue Segmentation lazy-mount au premier clic : table épisodes avec état (raw/normalisé/segmenté) | ✅ |
| E2 | Bouton «Segmenter» par épisode normalisé → job `segment_transcript` créé, badge «✓ en queue» | ✅ |
| E3 | «Segmenter tout» : crée jobs pour tous les épisodes normalisés, lance poll jobs | ✅ |

**Validation E1** : `activateSubView("segmentation")` → vérifie `.cons-loading` sentinel → `loadAndRenderSegmentation(container)`. Table avec badges `raw/normalized/segmented` (`constituerModule.ts:1467-1515`).

**Validation E2** : `constituerModule.ts:1500-1514` — `.seg-ep-btn` click → `createJob("segment_transcript", epId)` → `startJobPoll()` → btn.textContent = «✓ en queue».

**Validation E3** : `constituerModule.ts:2186-2199` — `#cons-batch-segment` click → filter `state === "normalized"` → `createJob` pour chaque → `startJobPoll()`.

---

### F. Actions — Alignement (3 items)

| # | Item | Statut |
|---|------|--------|
| F1 | Sous-vue Alignement lazy-mount : colonnes Segments + SRTs par épisode | ✅ |
| F2 | Épisode sans segments → indicateur «seg. manquante» (pas de bouton Aligner) | ✅ |
| F3 | Épisode avec segments + SRT → bouton «→ Aligner» → navigate Aligner avec handoff correct | ✅ |

**Validation F1** : `loadAndRenderAlignement()` → `renderAlignementPane()` → table avec colonnes ID / Titre / Segments / SRTs (`constituerModule.ts:1531-1591`).

**Validation F2** : `constituerModule.ts:1548` — si `!isSegmented` → affiche `"seg. manquante"` sans bouton Aligner.

**Validation F3** : `constituerModule.ts:1562-1578` — `.align-ep-btn` click → `setHandoff({episode_id, pivot_key:"transcript", target_keys:srtKeys, mode:"transcript_first", segment_kind:"utterance"})` → `navigateTo("aligner")`.

---

### G. Personnages (2 items)

| # | Item | Statut |
|---|------|--------|
| G1 | Section Personnages : liste + detail + sauvegarder → PUT /characters (régression MX-024) | ✅ |
| G2 | Assignations affichées dans le panneau détail personnage | ✅ |

**Validation G1** : `renderPersonnagesSection()` → `fetchCharacters()` + `saveCharacters()` → `PUT /characters`. Aucune régression : endpoint `/characters` opérationnel (commit 9086488).

**Validation G2** : `constituerModule.ts:1714` — `Promise.all([fetchCharacters(), fetchAssignments()])` → `_assignments` utilisé dans `renderCharDetail()` pour afficher les assignations par personnage.

---

### H. Exporter (3 items)

| # | Item | Statut |
|---|------|--------|
| H1 | Section Exporter dans Constituer : bouton corpus TXT → fichier `exports/corpus.txt` créé | ✅ |
| H2 | Section Exporter : bouton segments CSV → `exports/segments.csv` créé (12 segments projet démo) | ✅ |
| H3 | Vue Exporter top-level (tile Hub) : même fonctionnalité, nom projet affiché | ✅ |

**Validation H1-H2** : Validés via curl MX-026.

**Validation H3** : `exporterModule.ts` — module complet avec header projet (fetchConfig → badge nom), 2 cartes corpus/segments, boutons format → `runExport(scope, fmt)` → résultat inline.

---

### I. Concordancier (2 items)

| # | Item | Statut |
|---|------|--------|
| I1 | Recherche KWIC + filtre langue → résultats (régression MX-024) | ✅ |
| I2 | Filtre speaker (nom locuteur) → sous-ensemble cohérent des résultats | ✅ |

**Validation I1-I2** : Validés via curl MX-026 — `POST /query` opérationnel avec filtre speaker.

---

### J. Aligner (1 item)

| # | Item | Statut |
|---|------|--------|
| J1 | Cibles SRT : checkboxes pré-cochées, décochables ; lancement interdit si aucune cible (régression Écart #5) | ✅ |

**Validation J1** : `alignerModule.ts:427-431` — `if (targetLangs.length === 0)` → feedback «Sélectionnez au moins une cible SRT.» + `btn.disabled = false` (retour, pas de job). Checkboxes pré-cochées via `targetKeysDefault` (ligne 365).

---

### K. Inspecter (1 item)

| # | Item | Statut |
|---|------|--------|
| K1 | Auto-refresh liste épisodes après fin de job normalisation/segmentation (régression Écart #2) | ✅ |

**Validation K1** : `inspecterModule.ts:210-248` — `startJobPoll(container, jobId)` → `setInterval` 2s → si `job.status === "done"` → `loadEpisodes(container)`. Écart #2 corrigé.

---

### L. Style (1 item)

| # | Item | Statut |
|---|------|--------|
| L1 | Boutons compacts (.btn-sm) cohérents dans tous les modules : padding / font-size AGRAFES | ✅ |

**Validation L1** : `ui/dom.ts` — `.btn-sm { padding: 0.2rem 0.55rem; font-size: 0.78rem; }`. Appliqué dans alignerModule, concordancierModule, inspecterModule, constituerModule, exporterModule. TypeScript compile sans erreur.

---

## Résultat

| Statut | Compte |
|--------|--------|
| ✅ PASS | 25 / 25 |
| ❌ FAIL | 0 / 25 |
| ⬜ À valider UI | 0 / 25 |

**Verdict global : PASS — 25/25 items validés**

---

## Items backend validés (curl)

| Endpoint | Résultat |
|----------|---------|
| GET /config | `project_name`, `source_id`, `series_url`, `normalize_profile`, `languages` présents |
| PUT /config | Mise à jour acceptée, retour config cohérent |
| POST /web/tvmaze/discover `{"series_name":"Breaking Bad"}` | 62 épisodes — ✅ |
| POST /export `{"scope":"corpus","fmt":"txt"}` | `exports/corpus.txt` — 1 épisode démo |
| POST /export `{"scope":"segments","fmt":"csv"}` | `exports/segments.csv` — 12 segments démo |
| POST /query | Endpoint opérationnel, filtre speaker fonctionnel |

---

## Méthode de validation

Items A1-A2, B1-B3, C1, C4, E1-E3, F1-F3, G1-G2, H3, J1, K1, L1 : validation par inspection de code (TypeScript `--noEmit` propre + lecture des modules). Aucune régression détectée.

Items C2, C3, D1, H1-H2, I1-I2 : validés via curl backend.

---

## Écarts détectés

_(aucun)_
